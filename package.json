{
	"name": "mk-business",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"sass:dev": "sass assets/scss/theme.scss:style.css --source-map --watch --quiet-deps",
		"sass:build": "sass assets/scss/theme.scss:style.css --style=compressed --no-source-map --quiet-deps",
		"build": "npm run sass:build"
	},
	"dependencies": {
		"bootstrap": "^5.3.2"
	},
	"devDependencies": {
		"sass": "^1.69.5"
	}
}